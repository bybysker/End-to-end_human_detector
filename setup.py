from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='End-to-end pipeline of a human detector ML application',
    author='Elbby Skermine',
    license='MIT',
)
